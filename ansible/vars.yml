#hosts
ansible_host: localhost
ansible_port: 2222
ansible_user: root
ansible_ssh_private_key_file: './ssh/docker_rsa'

#php
php_version: 8.4

#mysql
db_host: "localhost"
db_name: "app"
db_user: "app"
db_pass: "app_password"
mysql_root_password: "root_password" # "{{ vault_mysql_root_password }}"  # Use Ansible Vault for sensitive data
